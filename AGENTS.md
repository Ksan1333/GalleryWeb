# 作業完了・リリース運用ルール

このプロジェクトでアプリに関する作業を完了するときは、以下を必須とする。途中までしか実施できない場合は「完了」と報告せず、未完了項目と理由を明記する。

## 競合防止

- 着手時と完了前に `git status` と差分を確認する。
- 他の作業者・タスクによる変更を上書き、破棄、ステージ、コミットしない。
- 自分の作業分だけを分離してコミットする。分離できない場合は作業を止めて報告する。

## 完了条件

1. **版数更新**
   - SemVerに従って次の版数を決め、プロジェクト内の全バージョン定義を一致させる。
   - 過去版の成果物、タグ、仕様書を上書き・削除しない。
2. **CHANGELOG**
   - `CHANGELOG.md` に版数、日付、具体的な変更内容を記録する。
   - Added / Changed / Fixed 等に分類し、利用者が影響を判断できる粒度で書く。
3. **アプリ仕様書**
   - 実装と一致するよう仕様書を更新する。
   - 既存仕様書を上書きせず、版数を含む別ファイル（例: `docs/APP_SPECIFICATION_v1.2.3.md`）として保存する。
   - 仕様書の版数はアプリ版数と一致させ、過去版も残す。
4. **配布物作成**
   - テストとビルドを実行して成功を確認する。
   - Tauriプロジェクトでは `package.json`、`Cargo.toml`、`tauri.conf.json` 等の版数を揃え、版数入りのWindowsインストーラーEXEを生成する。
   - Tauri以外では、APK、Electron EXE、Python EXE、NSIS等、そのプロジェクトの配布形式で版数入り成果物を生成する。
   - 版数なしの「最新版」ファイルを併設してもよいが、版数入り成果物を必ず残す。
5. **コミット・プッシュ**
   - CHANGELOG、版数別仕様書、ソース、ビルド設定を含む自分の変更をコミットする。
   - コミットメッセージには版数と変更内容を具体的に記載する。
   - 現在の正しいブランチをリモートへプッシュし、ローカルHEADとリモートが一致することを確認する。
6. **GitHub Release**
   - 版数に対応するタグとGitHub Releaseを作成する。
   - Release本文に変更内容、確認内容、既知の問題を記載する。
   - 版数入り成果物を添付し、可能な場合はSHA-256も記載する。
   - 公開後にタグ、本文、添付ファイル、下書き残りがないことを確認する。
7. **完了報告**
   - コミットID、プッシュ先ブランチ、Release URL、CHANGELOG、版数別仕様書、配布物のパスを報告する。

